{
  "contract_version": "1.0.3",
  "digest": "sha256:ba6f659e1178374df0f275fded3ed7c3a8899b59c4a12701f8fca2ed3a693ae8",
  "generated_with": {
    "upstream": {
      "pyOfficeEditor": "0.3.0",
      "pyOpenVBA": "6.1.2",
      "pyvbaanalysis": "2.2.1",
      "pyvbaharness": "1.1.5"
    }
  },
  "reference_implementation": "python",
  "server": {
    "instructions": "Read and change the VBA, UserForms, Power Query and worksheet cells inside Office files, and the document around them: Excel (.xlsm, .xlsb, .xlam, .xls), Word (.docm, .dotm, .doc), PowerPoint (.pptm, .potm) and Access (.accdb, .mdb), plus Power Query and sheets in plain .xlsx, and Visual Basic 6 projects (.vbp).\n\nA .vbp's modules are files on disk, so its writes land as they are made and it is never protected or signed. Everything else works the same way.\n\nReading and writing the file needs no Office installation and works on any platform. Running macros and tests needs Windows with the application installed.\n\nWorkflow\n\n1. xlide_list_projects when the user has not named a file. Pass absolute paths after that.\n2. xlide_project_info once per file. It lists modules, forms, queries and sheets, and says whether the project is password-protected or digitally signed.\n3. xlide_read_module to read. Its result carries a content_token; pass that back as expected_content_token to xlide_write_module and the write is refused if the module changed after your read.\n4. xlide_write_module to change code. It replaces the module's whole source, so send all of it. The attribute header is managed for you: send the body.\n5. xlide_analyze after every VBA change. Treat any error-severity problem as a build failure: fix it and analyze again until it is clean.\n6. xlide_run_tests when the file has tests and the change touches tested code.\n\nRules\n\n- The VBA inside the file is the only source of truth for it. Exported .bas and .cls files are copies and go stale. Never report a module as changed because you wrote an exported copy.\n- Do not unzip, rezip or hex-edit an Office file, and do not touch vbaProject.bin. Those edits corrupt the file or drop its code silently.\n- Do not drive Office with COM automation of your own. xlide_run_macro and xlide_run_tests use an instance this server owns and holds a deadline over. To open or close a file in the user's own application, use xlide_open_in_app and xlide_close_in_app, which act on that one file.\n- Read the code you are about to change, and change only what the task needs.\n- Ask the user before anything hard to undo: deleting a module, a form or a query, overwriting cells that hold data, writing to a project that is password-protected or digitally signed, closing a file that holds unsaved work, or ending a process. A signed project loses its signature on any macro change.\n- Keep VBA source ASCII unless the user asks otherwise.\n- To show the user what you changed, use the diff a write returns: it is taken from the file read back after saving, so it is what the file now holds rather than what you meant to write. xlide_git_changes does the same against a git revision, and covers Power Query as well as VBA. Neither compares cell values.\n- A write fails while the file is open in its Office application, and the refusal names what holds it. xlide_is_open says whether that copy is read-only and whether it holds unsaved work. A read-only copy with nothing unsaved loses nothing by being closed and reopened around the write; anything else is the user's to close.\n- A macro-enabled file saved before its first macro has no VBA project. That is normal: the listings answer empty, and xlide_write_module gives the file its project.\n- Access runs its compiled project, so a module written here takes effect when Access next opens the database.\n- Cell values read from the file are what Excel last calculated. A formula you write, and anything depending on a cell you write, has no current result in the file until Excel next opens it. xlide_read_cells with calculate=true works results out with a formula engine and names any cell it could not; report those as Excel's cached values.\n- When XLIDE for VS Code is running, a write's result carries xlide_vscode, and the user can see the change and keep or revert it in XLIDE's project tree. Say so.\n- Report what you changed, how you verified it, and what the user still has to do.\n",
    "name": "xlide"
  },
  "tool_count": 58,
  "tools": [
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists what an Access database holds besides its code: tables with their columns, saved queries with their SQL, and the relationships between tables. An .accdb is an application rather than a document, and the VBA in it is written against these, so reading the modules alone shows half of it. Access files only.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Access database.",
            "title": "File Path",
            "type": "string"
          },
          "include": {
            "default": "all",
            "description": "'tables', 'queries', 'relationships' or 'all'.",
            "title": "Include",
            "type": "string"
          },
          "include_system": {
            "default": false,
            "description": "Include the MSys tables and relationships Access keeps its own objects in. Off by default: every database has them, and listing them buries the few that are about the user's data.",
            "title": "Include System",
            "type": "boolean"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "access_catalogArguments",
        "type": "object"
      },
      "name": "xlide_access_catalog",
      "title": "Access tables and queries"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": false,
        "read_only": false
      },
      "description": "Adds a chart of a block of cells to a worksheet and saves the workbook, written as Excel's Insert Chart writes one of the same type. The block's first row names the series and its first column holds the categories, as Excel reads a selection; a block taller than it is wide makes a series of each column. The data can be on another sheet, as Data!A1:C13. Place it with cell or left and top in points. xlide_list_shapes reports it afterwards with each series' formula.",
      "input_schema": {
        "properties": {
          "cell": {
            "default": "",
            "description": "The chart's top-left cell, such as H2.",
            "title": "Cell",
            "type": "string"
          },
          "chart_name": {
            "default": "",
            "description": "Its name. Empty gives Chart 1, Chart 2 and so on.",
            "title": "Chart Name",
            "type": "string"
          },
          "chart_type": {
            "default": "column",
            "description": "column, bar, line, lineMarkers, pie, doughnut, scatter or area.",
            "title": "Chart Type",
            "type": "string"
          },
          "data_range": {
            "description": "The block to chart, header row and category column included.",
            "title": "Data Range",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "height": {
            "default": 0.0,
            "description": "Points. 0 gives Excel's 216.",
            "minimum": 0,
            "title": "Height",
            "type": "number"
          },
          "left": {
            "default": -1.0,
            "description": "Without cell: points from the left.",
            "title": "Left",
            "type": "number"
          },
          "series_in": {
            "default": "",
            "description": "'columns' or 'rows' to say which way the series run. Empty decides.",
            "title": "Series In",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet the chart goes on.",
            "title": "Sheet",
            "type": "string"
          },
          "title": {
            "default": "",
            "description": "The title. Empty keeps Excel's automatic one.",
            "title": "Title",
            "type": "string"
          },
          "top": {
            "default": -1.0,
            "description": "Without cell: points from the top.",
            "title": "Top",
            "type": "number"
          },
          "width": {
            "default": 0.0,
            "description": "Points. 0 gives Excel's 360.",
            "minimum": 0,
            "title": "Width",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "data_range"
        ],
        "title": "add_chartArguments",
        "type": "object"
      },
      "name": "xlide_add_chart",
      "title": "Add a chart"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Runs static analysis over every VBA module in an Office file and returns the problems with module, line, column, code and message. Needs no Office installation and runs nothing. Each file is measured against its own host's object model, so Word code is never judged by Excel's surface, and a Visual Basic 6 project against no host at all, because its code does not run in one. Call this after every VBA change and treat any problem at error severity as a build failure: fix it and analyze again until it is clean. Warnings are worth reading; some are style, some are the bug.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "min_severity": {
            "default": "information",
            "description": "Lowest severity to report: 'error', 'warning' or 'information'.",
            "title": "Min Severity",
            "type": "string"
          },
          "module_name": {
            "default": "",
            "description": "Report only this module's problems. The whole project is still analyzed, so cross-module references still resolve.",
            "title": "Module Name",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "analyzeArguments",
        "type": "object"
      },
      "name": "xlide_analyze",
      "title": "Analyze VBA"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Runs static analysis over VBA source you are holding, before it is written to a file. Use it to check code you have just generated: it costs nothing, needs no file, and catches the compile errors that would otherwise surface in front of the user. Pass host so the code is measured against the right object model, and file_path instead if the code is destined for a file that already exists, which resolves calls into the rest of that project.",
      "input_schema": {
        "properties": {
          "file_path": {
            "default": "",
            "description": "An existing Office file this module belongs to. Its other modules are analyzed alongside, so calls into them resolve. Overrides host.",
            "title": "File Path",
            "type": "string"
          },
          "host": {
            "default": "",
            "description": "'excel', 'word', 'powerpoint' or 'access'. Empty checks the language alone, with no host object model.",
            "title": "Host",
            "type": "string"
          },
          "kind": {
            "default": "standard",
            "description": "'standard', 'class', 'document' or 'userform'.",
            "title": "Kind",
            "type": "string"
          },
          "module_name": {
            "default": "Module1",
            "description": "Name to report problems against.",
            "title": "Module Name",
            "type": "string"
          },
          "source": {
            "description": "The VBA source to check.",
            "title": "Source",
            "type": "string"
          }
        },
        "required": [
          "source"
        ],
        "title": "analyze_sourceArguments",
        "type": "object"
      },
      "name": "xlide_analyze_source",
      "title": "Analyze VBA source"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": false,
        "read_only": false
      },
      "description": "Closes a file in the Office application the user has it open in, which frees it for a write. Only that file: the application stays, unless this server started the instance and nothing else is open in it. A copy holding unsaved work is left open and reported, unless save_changes saves it first or discard_changes closes it without saving; losing the user's work is their decision, so ask them. When the application does not answer, or the file stays locked, end_process ends the process Windows says holds the file, and every unsaved document in that process is lost with it: ask the user first. Where the user was in the file is remembered, and xlide_open_in_app puts them back there. Windows only.",
      "input_schema": {
        "properties": {
          "discard_changes": {
            "default": false,
            "description": "Close without saving, losing unsaved changes. Ask the user first.",
            "title": "Discard Changes",
            "type": "boolean"
          },
          "end_process": {
            "default": false,
            "description": "If the application does not answer, or the file stays locked, end the process holding it. Loses unsaved work in every document open in it. Ask the user first.",
            "title": "End Process",
            "type": "boolean"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "save_changes": {
            "default": false,
            "description": "Save unsaved changes before closing. Not for a read-only copy.",
            "title": "Save Changes",
            "type": "boolean"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "close_in_appArguments",
        "type": "object"
      },
      "name": "xlide_close_in_app",
      "title": "Close in Office"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Asks the real VBA editor to compile a file's project, and reports whether it accepted it. Windows only. This is the compiler's own verdict, where xlide_analyze is a static analyzer's; run the analyzer first, because it is free and needs no Office, and use this when the last word matters. Excel is made visible for the check, because a hidden instance does not surface the compile-error dialog and would report a rejection as a pass.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds to wait for the verdict.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "compile_checkArguments",
        "type": "object"
      },
      "name": "xlide_compile_check",
      "title": "Compile check"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": false,
        "read_only": false
      },
      "description": "Creates a new Office file with an empty VBA project at the given absolute path, from a template the application itself authored, so it opens with no repair prompt. Extensions: .xlsm, .xlsb, .xlam, .docm, .pptm, .accdb, and .xlsx for a workbook with Power Query and no macros. It never overwrites a file or replaces a project that is already there. A .xlsm, .docm or .pptm saved before its first macro has no VBA project at all; xlide_write_module gives it one along with the first module, and this tool gives an existing .docm the empty one Word makes.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to create, whose extension picks the format, or an existing .docm with no VBA project.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "create_projectArguments",
        "type": "object"
      },
      "name": "xlide_create_project",
      "title": "Create Office file or VBA project"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": false,
        "read_only": false
      },
      "description": "Permanently deletes a VBA module from an Office file and saves it. There is no undo: ask the user first, and read the module before deleting it so its code can be put back if they change their mind. Document modules cannot be deleted.",
      "input_schema": {
        "properties": {
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "expected_content_token": {
            "default": "",
            "description": "The content_token from your read, so a module that changed since is not deleted on the strength of a stale look at it.",
            "title": "Expected Content Token",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "module_name": {
            "description": "Module to delete.",
            "title": "Module Name",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "module_name"
        ],
        "title": "delete_moduleArguments",
        "type": "object"
      },
      "name": "xlide_delete_module",
      "title": "Delete module"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reports what this server can reach: its workspace roots, whether it is read-only, which optional layers are installed, and, on Windows, which Office applications are available to run macros and tests. Call it when a tool reports something missing, or before promising the user a test run.",
      "input_schema": {
        "properties": {},
        "title": "doctorArguments",
        "type": "object"
      },
      "name": "xlide_doctor",
      "title": "What this machine can do"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Changes one form's design and saves the file: add a control, remove one, or set a property on one. Geometry is in points for a UserForm and twips for an Access design. Setting a property to null clears it, putting the control back at its default. Removing a container takes its children with it, so ask the user first. Adding a control does not write its event procedure; do that with xlide_write_module on the form's code module.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "'add_control', 'remove_control' or 'set_property'.",
            "title": "Action",
            "type": "string"
          },
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "caption": {
            "default": "",
            "description": "For add_control on an Access design: the control's caption.",
            "title": "Caption",
            "type": "string"
          },
          "container": {
            "default": "",
            "description": "For add_control: the Frame or page to put it in.",
            "title": "Container",
            "type": "string"
          },
          "control_name": {
            "description": "The control to add, remove or change.",
            "title": "Control Name",
            "type": "string"
          },
          "control_type": {
            "default": "",
            "description": "For add_control: Label, TextBox, CommandButton, CheckBox, OptionButton, ComboBox, ListBox, Frame, MultiPage, Image, SpinButton and the rest.",
            "title": "Control Type",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "form_name": {
            "description": "Form to change.",
            "title": "Form Name",
            "type": "string"
          },
          "height": {
            "default": 0.0,
            "description": "0 uses the type's default.",
            "title": "Height",
            "type": "number"
          },
          "left": {
            "default": 6.0,
            "description": "For add_control.",
            "title": "Left",
            "type": "number"
          },
          "property_name": {
            "default": "",
            "description": "For set_property: the property to set.",
            "title": "Property Name",
            "type": "string"
          },
          "property_value": {
            "default": null,
            "description": "For set_property. null clears it to the default.",
            "title": "Property Value"
          },
          "section": {
            "default": "",
            "description": "For add_control on an Access design: the band to put it in, such as Detail, PageHeaderSection or PageFooterSection. xlide_list_forms names the ones a design has. Ignored for a UserForm, which has no bands.",
            "title": "Section",
            "type": "string"
          },
          "top": {
            "default": 6.0,
            "description": "For add_control.",
            "title": "Top",
            "type": "number"
          },
          "width": {
            "default": 0.0,
            "description": "0 uses the type's default.",
            "title": "Width",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "form_name",
          "action",
          "control_name"
        ],
        "title": "edit_formArguments",
        "type": "object"
      },
      "name": "xlide_edit_form",
      "title": "Edit form design"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Works out what a formula would give in a cell of a workbook, without writing it there: to check a formula before xlide_write_cells, or to ask the workbook a question, such as =SUMIFS(Sales[Amount],Sales[Region],\"West\"). Uses pyOfficeEditor's formula engine, 493 of Excel's functions, with the workbook's cells as they stand. A formula written for one cell answers with one value, as Excel would give it there. Changes nothing. .xlsx, .xlsm and .xlam.",
      "input_schema": {
        "properties": {
          "at": {
            "default": "A1",
            "description": "The cell it is imagined in, which relative references count from.",
            "title": "At",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "formula": {
            "description": "The formula, with or without its '='.",
            "title": "Formula",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet the formula is evaluated on.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "formula"
        ],
        "title": "evaluate_formulaArguments",
        "type": "object"
      },
      "name": "xlide_evaluate_formula",
      "title": "Evaluate a formula"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Writes every VBA module in an Office file to a folder as .bas and .cls files, for source control or review. Previews by default: it reports what it would create, update and leave alone, and writes nothing until apply=true. Only when the user asks for it. The exported files are copies: editing one changes nothing inside the Office file until xlide_import_modules runs.",
      "input_schema": {
        "properties": {
          "apply": {
            "default": false,
            "description": "Write the files. False previews the plan.",
            "title": "Apply",
            "type": "boolean"
          },
          "delete_stale": {
            "default": false,
            "description": "Delete .bas and .cls files in the folder that match no module. Ask the user first.",
            "title": "Delete Stale",
            "type": "boolean"
          },
          "export_folder": {
            "default": "",
            "description": "Folder to write into. Empty uses a folder named after the file, beside it.",
            "title": "Export Folder",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "export_modulesArguments",
        "type": "object"
      },
      "name": "xlide_export_modules",
      "title": "Export modules to files"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Changes how a range looks: bold and font, fill colour, borders, alignment, number format, and merging. Only what you pass is changed, so each cell keeps the rest of its own format and making a header row bold does not flatten the number formats under it. Colours are RRGGBB or AARRGGBB hex without a leading hash. A number format is an Excel format code such as '#,##0.00' or 'yyyy-mm-dd'; that code is also what decides whether a number is shown as a date. Works on .xlsx, .xlsm and .xlam.",
      "input_schema": {
        "properties": {
          "bold": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Bold on or off.",
            "title": "Bold"
          },
          "border_color": {
            "default": "",
            "description": "Border colour as hex. Defaults to automatic.",
            "title": "Border Color",
            "type": "string"
          },
          "border_style": {
            "default": "",
            "description": "Border on all four sides: thin, medium, thick, double, dotted, dashed, hair, or none to remove.",
            "title": "Border Style",
            "type": "string"
          },
          "cell_range": {
            "description": "A1-style range, such as A1:D1, or a single cell.",
            "title": "Cell Range",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "fill_color": {
            "default": "",
            "description": "Solid background colour as hex. 'none' clears the fill back to no fill.",
            "title": "Fill Color",
            "type": "string"
          },
          "font_color": {
            "default": "",
            "description": "Text colour as hex, such as 'FF0000'.",
            "title": "Font Color",
            "type": "string"
          },
          "font_name": {
            "default": "",
            "description": "Typeface, such as 'Calibri'.",
            "title": "Font Name",
            "type": "string"
          },
          "font_size": {
            "default": 0,
            "description": "Points. 0 leaves it alone.",
            "minimum": 0,
            "title": "Font Size",
            "type": "number"
          },
          "horizontal": {
            "default": "",
            "description": "left, center, right, fill, justify or general.",
            "title": "Horizontal",
            "type": "string"
          },
          "italic": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Italic on or off.",
            "title": "Italic"
          },
          "merge": {
            "default": "",
            "description": "'merge' joins the range into one cell, keeping only the top-left value. 'unmerge' splits it back. Empty leaves merging alone.",
            "title": "Merge",
            "type": "string"
          },
          "number_format": {
            "default": "",
            "description": "Excel format code, such as '#,##0.00', '0%' or 'yyyy-mm-dd'.",
            "title": "Number Format",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "strike": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Strikethrough on or off.",
            "title": "Strike"
          },
          "style": {
            "default": "",
            "description": "A named cell style, applied first: one of Excel's own, such as Good, Bad, Neutral, Title, Heading 1, Total, Input, Output, Note or 20% - Accent1, or one the workbook defines. It sets only the parts the style includes.",
            "title": "Style",
            "type": "string"
          },
          "underline": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Single underline on or off.",
            "title": "Underline"
          },
          "vertical": {
            "default": "",
            "description": "top, center, bottom, justify or distributed.",
            "title": "Vertical",
            "type": "string"
          },
          "wrap_text": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Wrap text in the cell.",
            "title": "Wrap Text"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "cell_range"
        ],
        "title": "format_cellsArguments",
        "type": "object"
      },
      "name": "xlide_format_cells",
      "title": "Format cells"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists what changed inside an Office file since a git revision, one entry per VBA module and Power Query, with a unified diff. git diff cannot show this: the file is binary, so a commit that changed one line and one that replaced the whole project look the same. Call this before committing, and to review what an agent or a colleague changed. Worksheet cell values are not compared. Defaults to HEAD; pass any revision git understands. Needs the file to be inside a git repository and tracked in that revision.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "include_diff": {
            "default": true,
            "description": "Include the unified diff. Off gives just what changed and by how much.",
            "title": "Include Diff",
            "type": "boolean"
          },
          "revision": {
            "default": "HEAD",
            "description": "Any revision git understands: HEAD, a branch, a tag, a SHA.",
            "title": "Revision",
            "type": "string"
          },
          "section": {
            "default": "",
            "description": "Only this one, named as a module or a query is named. Empty reports every one.",
            "title": "Section",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "git_changesArguments",
        "type": "object"
      },
      "name": "xlide_git_changes",
      "title": "What changed inside the file"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Reads .bas and .cls files from a folder back into an Office file's VBA project and saves it. Previews by default: it reports which modules would change and by how much, and writes nothing until apply=true. A file whose name matches no module creates one. Document modules such as ThisWorkbook are written but never created. After applying, call xlide_analyze.",
      "input_schema": {
        "properties": {
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "apply": {
            "default": false,
            "description": "Write the modules. False previews the plan.",
            "title": "Apply",
            "type": "boolean"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "source_folder": {
            "description": "Folder holding the .bas/.cls files.",
            "title": "Source Folder",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "source_folder"
        ],
        "title": "import_modulesArguments",
        "type": "object"
      },
      "name": "xlide_import_modules",
      "title": "Import modules from files"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Says whether a file is open in its Office application, in any running instance: read-only or for editing, with unsaved changes or not, and which processes hold it locked. Call it when a write reports the file is locked, before closing anything, or when the user asks. Windows only. Changes nothing.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "is_openArguments",
        "type": "object"
      },
      "name": "xlide_is_open",
      "title": "Is the file open in Office"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the UserForms in an Office file, or the forms and reports in an Access database, with each one's control count and, for Access, the sections a control can go in. A design's code is a module of the same name, read with xlide_read_module.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_formsArguments",
        "type": "object"
      },
      "name": "xlide_list_forms",
      "title": "List forms"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the VBA modules in an Office file: name, kind (standard, class, document or userform), line count, and a content_token to pass to a guarded write. xlide_project_info returns this and more in one call; use this one when the file is already known and only the module list is wanted.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_modulesArguments",
        "type": "object"
      },
      "name": "xlide_list_modules",
      "title": "List modules"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the Sub, Function and Property procedures in one module, with each one's kind, scope, line number and signature. Use it to find where to change something without reading a long module in full.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "module_name": {
            "description": "Module name, matched without case.",
            "title": "Module Name",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "module_name"
        ],
        "title": "list_proceduresArguments",
        "type": "object"
      },
      "name": "xlide_list_procedures",
      "title": "List procedures"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Finds the Office files in this server's workspace and returns their absolute paths, host application and whether their VBA can be opened. Call this first when the user has not named a file. Covers Excel (.xlsm, .xlsb, .xlam, .xls, and .xlsx for Power Query and sheets), Word (.docm, .dotm, .doc), PowerPoint (.pptm, .potm), Access (.accdb, .mdb), and Visual Basic 6 projects (.vbp, whose modules are the files its manifest names). Files whose extension is recognized but not openable are listed with the reason, so a template or add-in is not silently missing.",
      "input_schema": {
        "properties": {
          "subfolder": {
            "default": "",
            "description": "Limit the search to this folder. Empty searches every workspace root.",
            "title": "Subfolder",
            "type": "string"
          }
        },
        "title": "list_projectsArguments",
        "type": "object"
      },
      "name": "xlide_list_projects",
      "title": "List Office files"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the Power Query queries in an Excel workbook: name, group, description, where each loads, and its applied step names. Queries live outside the VBA project, so a plain .xlsx has them too. Call this when asked what a workbook does; a workbook with no macros can still be doing most of its work here.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel workbook.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_queriesArguments",
        "type": "object"
      },
      "name": "xlide_list_queries",
      "title": "List Power Query"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the type libraries a VBA project references: the name code uses to qualify them, the kind, and the registry or file path the project recorded. Call it when a name will not resolve, when deciding between early and late binding, or when code works on one machine and not another: a reference names a path and a version, so a project can be broken by the machine rather than by its own code.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_referencesArguments",
        "type": "object"
      },
      "name": "xlide_list_references",
      "title": "List project references"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists what sits on a worksheet's drawing layer: buttons, form controls, AutoShapes, text boxes, pictures, charts and groups, each with the cells it covers, its position in points and, where it has one, the macro a click runs. A form control also carries what it holds: a check box's state, a list's chosen item, a spinner's value and bounds, the cell it is linked to. Call it when asked how a workbook is started, or before renaming a Sub: a button's OnAction names a procedure and nothing rewrites it. ActiveX controls are listed but have no macro; their code is event procedures in the sheet's module.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "sheet": {
            "default": "",
            "description": "One worksheet. Empty lists every sheet's shapes.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_shapesArguments",
        "type": "object"
      },
      "name": "xlide_list_shapes",
      "title": "List shapes and buttons"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the worksheets in an Excel file with their used ranges, whether each is hidden, and the pivot tables on each, the chart sheets, and the workbook's named ranges. Call this before reading cells, so the range you ask for is one that holds data. Works on .xlsx, .xlsm and .xlam; a .xlsb or .xls keeps its grid in a binary format, and Excel answers for those on Windows.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds allowed if Excel has to answer.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "list_sheetsArguments",
        "type": "object"
      },
      "name": "xlide_list_sheets",
      "title": "List worksheets"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reads a module's text as the running editor holds it, including edits the user has not saved. This is the one read that can differ from xlide_read_module, and the difference is exactly the user's unsaved work. Use it to see what they are actually working on; use xlide_read_module for what is in the file. surface_only=true reads the modern editor's own copy instead, which exists only for a module the user has open in a tab.",
      "input_schema": {
        "properties": {
          "module_name": {
            "description": "Module name as the editor shows it.",
            "title": "Module Name",
            "type": "string"
          },
          "pid": {
            "default": 0,
            "description": "Which session, by process id.",
            "title": "Pid",
            "type": "integer"
          },
          "project": {
            "default": "",
            "description": "Which project, when two documents are open and both hold a module of this name.",
            "title": "Project",
            "type": "string"
          },
          "surface_only": {
            "default": false,
            "description": "Read the modern editor's own copy, which only exists for an open tab. Off reads the module through the editor, which always answers.",
            "title": "Surface Only",
            "type": "boolean"
          }
        },
        "required": [
          "module_name"
        ],
        "title": "live_read_moduleArguments",
        "type": "object"
      },
      "name": "xlide_live_read_module",
      "title": "Read a module from the editor"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Calls one read route on a running xlide_vbide session and returns its JSON. Start with route='agent', which answers with the session's own route table and what each one is for. Allowed routes: agent, agent/examples, agent/routes, analyzer, doctor, engine, model, native, project, projects, state, stats, windows. Routes that drive the editor are deliberately not reachable here.",
      "input_schema": {
        "properties": {
          "pid": {
            "default": 0,
            "description": "Which session, by process id.",
            "title": "Pid",
            "type": "integer"
          },
          "query": {
            "default": "",
            "description": "Query string for the route, such as 'name=Module1' or 'type=Range'.",
            "title": "Query",
            "type": "string"
          },
          "route": {
            "description": "One of the read routes listed above.",
            "title": "Route",
            "type": "string"
          }
        },
        "required": [
          "route"
        ],
        "title": "live_requestArguments",
        "type": "object"
      },
      "name": "xlide_live_request",
      "title": "Query a live session"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Lists the running xlide_vbide sessions this machine can reach: one per Office process that has opened the Visual Basic Editor with the add-in loaded. Use it to find out whether the user has a live editor before asking about what is on their screen. Windows only, and the add-in's local API has to be switched on by the user. Each session is probed before it is listed, because a discovery file outlives the process that wrote it.",
      "input_schema": {
        "properties": {},
        "title": "live_sessionsArguments",
        "type": "object"
      },
      "name": "xlide_live_sessions",
      "title": "Live VBE sessions"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "What the running Visual Basic Editor is doing right now: the module and project on screen, whether the project is in break mode, whether there are unsaved edits, the procedure the caret is in, and whether the analyzer engine is answering. Use it when the user asks about what they are looking at, or before suggesting an edit to a module they may have unsaved changes in.",
      "input_schema": {
        "properties": {
          "pid": {
            "default": 0,
            "description": "Which session, by process id. 0 uses the only one, or refuses.",
            "title": "Pid",
            "type": "integer"
          }
        },
        "title": "live_stateArguments",
        "type": "object"
      },
      "name": "xlide_live_state",
      "title": "Live editor state"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, writes, answers or removes the comments on a worksheet's cells. Excel has two kinds: a note, the yellow box that shows on hover, and a threaded comment, the conversation on the Review tab with replies and a resolved state. action='list' reads both and changes nothing. 'set' writes a note, replacing one already on the cell, or with kind='thread' starts a conversation; 'reply' and 'resolve' act on a thread; 'remove' takes either kind off the cell. Use a note to explain a cell to whoever reads the workbook next.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, set, reply, resolve or remove.",
            "title": "Action",
            "type": "string"
          },
          "author": {
            "default": "Agent",
            "description": "Who wrote it, as the workbook shows the name.",
            "title": "Author",
            "type": "string"
          },
          "cell": {
            "default": "",
            "description": "The cell, such as B4. For list, empty lists the sheet.",
            "title": "Cell",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "kind": {
            "default": "note",
            "description": "For set: 'note' or 'thread'.",
            "title": "Kind",
            "type": "string"
          },
          "resolved": {
            "default": true,
            "description": "For resolve: false opens a thread again.",
            "title": "Resolved",
            "type": "boolean"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "text": {
            "default": "",
            "description": "For set and reply: what the comment says.",
            "title": "Text",
            "type": "string"
          },
          "visible": {
            "default": false,
            "description": "For a note: shown all the time, not only on hover.",
            "title": "Visible",
            "type": "boolean"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "manage_commentArguments",
        "type": "object"
      },
      "name": "xlide_manage_comment",
      "title": "Notes and comments"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, adds or clears conditional formatting: the rules that colour cells by what is in them. rule='cell_is' with an operator and a value paints cells that compare true; rule='expression' takes a formula written for the top-left cell of the range and applied relatively, the way Excel's 'Use a formula' box works; rule='color_scale' and 'data_bar' are the gradient and in-cell bar. The paint itself is the fill and font arguments, which is what Excel calls a differential format.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, add or clear.",
            "title": "Action",
            "type": "string"
          },
          "bold": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Make matching cells bold.",
            "title": "Bold"
          },
          "cell_range": {
            "default": "",
            "description": "For add and clear: the range. Empty clears the whole sheet.",
            "title": "Cell Range",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "fill_color": {
            "default": "",
            "description": "Fill to paint matching cells, as hex.",
            "title": "Fill Color",
            "type": "string"
          },
          "font_color": {
            "default": "",
            "description": "Text colour for matching cells, as hex.",
            "title": "Font Color",
            "type": "string"
          },
          "formula": {
            "default": "",
            "description": "For expression: a formula for the range's top-left cell.",
            "title": "Formula",
            "type": "string"
          },
          "operator": {
            "default": "greaterThan",
            "description": "For cell_is: greaterThan, between, etc.",
            "title": "Operator",
            "type": "string"
          },
          "other_value": {
            "default": "",
            "description": "For cell_is with between: the second value.",
            "title": "Other Value",
            "type": "string"
          },
          "rule": {
            "default": "cell_is",
            "description": "cell_is, expression, color_scale or data_bar.",
            "title": "Rule",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "value": {
            "default": "",
            "description": "For cell_is: the value compared against.",
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "manage_conditional_formatArguments",
        "type": "object"
      },
      "name": "xlide_manage_conditional_format",
      "title": "Conditional formatting"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, sets, reapplies or clears the autofilter on a worksheet or on an Excel table, and hides the rows it filters out. Excel does not apply a filter when it opens a workbook: it shows the rows as the file marks them, so the rows are worked out here, held to what Excel keeps. action='list' reads the filters and changes nothing. 'set' filters one column and keeps what the others already filter by; call it once per column. Give criteria as VBA's Range.AutoFilter spells Criteria1 and Criteria2: '>=10', '=North', '=*east*', '=' for blanks, '<>' for anything; or top for the top N items (negative for the bottom), or dynamic for aboveAverage, thisMonth, Q1 and the like. 'reapply' filters again after the data changed; 'clear' removes the filter and shows every row.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, set, reapply or clear.",
            "title": "Action",
            "type": "string"
          },
          "cell_range": {
            "default": "",
            "description": "For set on a sheet: the block with its header row, such as A1:F200. Empty keeps the range of the filter already there.",
            "title": "Cell Range",
            "type": "string"
          },
          "column": {
            "default": "",
            "description": "For set: the column, by header text or by number from 1 in the range.",
            "title": "Column",
            "type": "string"
          },
          "criteria1": {
            "default": "",
            "description": "For set: Criteria1, such as '>=10' or '=West'.",
            "title": "Criteria1",
            "type": "string"
          },
          "criteria2": {
            "default": "",
            "description": "For set: Criteria2, such as '<100'.",
            "title": "Criteria2",
            "type": "string"
          },
          "dynamic": {
            "default": "",
            "description": "For set: aboveAverage, belowAverage, today, yesterday, tomorrow, thisWeek, lastWeek, nextWeek, thisMonth, lastMonth, nextMonth, thisQuarter, lastQuarter, nextQuarter, thisYear, lastYear, nextYear, yearToDate, Q1 to Q4 or M1 to M12.",
            "title": "Dynamic",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "match_all": {
            "default": true,
            "description": "With two criteria: true keeps rows meeting both, false either.",
            "title": "Match All",
            "type": "boolean"
          },
          "percent": {
            "default": false,
            "description": "top counts percent rather than items.",
            "title": "Percent",
            "type": "boolean"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "table": {
            "default": "",
            "description": "Filter this Excel table rather than a range.",
            "title": "Table",
            "type": "string"
          },
          "top": {
            "default": 0,
            "description": "For set: keep the top N items, or with a negative N the bottom N.",
            "title": "Top",
            "type": "integer"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "manage_filterArguments",
        "type": "object"
      },
      "name": "xlide_manage_filter",
      "title": "Autofilters"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Creates a UserForm, or an Access form or report, and saves the file. A form is a designer storage and a code module of the same name, and this writes both, which is why it exists rather than xlide_write_module. Renaming and deleting work on Access designs, where both halves move together; for a UserForm they are refused, because nothing here can move the designer storage and doing half of it loses the form. Add controls afterwards with xlide_edit_form, and write its event procedures with xlide_write_module.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "'create', 'rename' or 'delete'.",
            "title": "Action",
            "type": "string"
          },
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "caption": {
            "default": "",
            "description": "For create: the caption it opens with.",
            "title": "Caption",
            "type": "string"
          },
          "design": {
            "default": "form",
            "description": "For create in Access: 'form' or 'report'. Elsewhere, a form.",
            "title": "Design",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "form_name": {
            "description": "The form or report to act on.",
            "title": "Form Name",
            "type": "string"
          },
          "height": {
            "default": 0.0,
            "description": "For create.",
            "title": "Height",
            "type": "number"
          },
          "new_name": {
            "default": "",
            "description": "For rename: the new name.",
            "title": "New Name",
            "type": "string"
          },
          "width": {
            "default": 0.0,
            "description": "For create. Points for a UserForm, twips for Access.",
            "title": "Width",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "action",
          "form_name"
        ],
        "title": "manage_formArguments",
        "type": "object"
      },
      "name": "xlide_manage_form",
      "title": "Create, rename or delete a form"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, adds or removes hyperlinks on a worksheet. A link either goes out to a target, which is a URL or a file path, or inside the workbook to a location such as 'Summary!A1'. Pass one or the other. The cell's displayed text is separate from the link and is left alone unless you pass display.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, add or remove.",
            "title": "Action",
            "type": "string"
          },
          "cell_range": {
            "default": "",
            "description": "For add and remove: the cell or range.",
            "title": "Cell Range",
            "type": "string"
          },
          "display": {
            "default": "",
            "description": "For add: text to put in the cell.",
            "title": "Display",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "location": {
            "default": "",
            "description": "For add: a place in this workbook, like Summary!A1.",
            "title": "Location",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "target": {
            "default": "",
            "description": "For add: a URL or file path to open.",
            "title": "Target",
            "type": "string"
          },
          "tooltip": {
            "default": "",
            "description": "For add: the hover text.",
            "title": "Tooltip",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "manage_hyperlinkArguments",
        "type": "object"
      },
      "name": "xlide_manage_hyperlink",
      "title": "Hyperlinks"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, adds or removes a workbook's defined names. A defined name is what lets a formula say TaxRate rather than Config!$B$7, and VBA reads them too, so renaming or removing one can break code as well as formulas. action='list' changes nothing. refers_to is a formula, so it needs its sheet and its dollar signs: Data!$A$1:$A$50.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, add or remove.",
            "title": "Action",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "name": {
            "default": "",
            "description": "For add and remove: the defined name.",
            "title": "Name",
            "type": "string"
          },
          "refers_to": {
            "default": "",
            "description": "For add: what it points at, such as Data!$A$1:$A$50.",
            "title": "Refers To",
            "type": "string"
          },
          "scope": {
            "default": "",
            "description": "A sheet name to scope it to that sheet. Empty means the workbook.",
            "title": "Scope",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "manage_nameArguments",
        "type": "object"
      },
      "name": "xlide_manage_name",
      "title": "Defined names"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Adds or removes a type library reference in a VBA project and saves the file. Use it when xlide_analyze reports missing-library-reference, or when code needs early binding, as Dim d As Scripting.Dictionary does. Name the library: Excel, Word, PowerPoint and Access work anywhere, and on Windows any library the VBA editor's References dialog lists resolves by its description (Microsoft Scripting Runtime) or by the name code writes (Scripting), taking its GUID, version and path from this machine's registry. A name that matches several libraries is refused with the candidates. Off Windows, pass guid and version. Adding the host's own library, or removing Microsoft Forms while a UserForm exists, is refused: the first is implicit and the second stops the project compiling.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "'add' or 'remove'.",
            "title": "Action",
            "type": "string"
          },
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "guid": {
            "default": "",
            "description": "For add: the library's GUID, when it is not registered on this machine. library is then the name the reference is recorded under.",
            "title": "Guid",
            "type": "string"
          },
          "library": {
            "description": "For add: the library's description, the name code uses, or Excel, Word, PowerPoint or Access. For remove: the name xlide_list_references reports, or the GUID.",
            "title": "Library",
            "type": "string"
          },
          "version": {
            "default": "",
            "description": "For add with guid: major.minor in hexadecimal, as the registry spells it, such as 1.0 or 2.8. Empty takes the newest registered, or 1.0.",
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "action",
          "library"
        ],
        "title": "manage_referenceArguments",
        "type": "object"
      },
      "name": "xlide_manage_reference",
      "title": "Add or remove a project reference"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Inserts, deletes, resizes, hides, shows, groups or ungroups whole rows or columns. Inserting and deleting move every reference in the workbook with them: a formula pointing below an inserted row follows it, and one pointing into deleted cells becomes #REF!, exactly as Excel does it. Deleting has no undo, so ask the user first. Widths are in characters and heights in points, which is what Excel's own dialogs use. Works on .xlsx, .xlsm and .xlam.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "insert, delete, resize, hide, show, group or ungroup.",
            "title": "Action",
            "type": "string"
          },
          "collapsed": {
            "default": false,
            "description": "For group: start the new group collapsed.",
            "title": "Collapsed",
            "type": "boolean"
          },
          "count": {
            "default": 1,
            "description": "How many, counting from first.",
            "minimum": 1,
            "title": "Count",
            "type": "integer"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "first": {
            "description": "First row or column, 1-based. Column A is 1.",
            "minimum": 1,
            "title": "First",
            "type": "integer"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "size": {
            "default": -1,
            "description": "For resize: column width in characters, or row height in points. -1 restores the sheet default.",
            "title": "Size",
            "type": "number"
          },
          "which": {
            "description": "'rows' or 'columns'.",
            "title": "Which",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "action",
          "which",
          "first"
        ],
        "title": "manage_rows_columnsArguments",
        "type": "object"
      },
      "name": "xlide_manage_rows_columns",
      "title": "Insert, delete or size rows and columns"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Adds a Forms control, an AutoShape, a text box, a line or a picture to a worksheet, or removes one, and saves the workbook. The usual case is a button that runs a macro: kind='button', a caption in text, and the procedure in macro as Proc or Module.Proc, written first. Place it with cell, its top-left cell, or with left and top in points; width and height are points, with a size to start from when left at 0. Check boxes, option buttons, lists and drop-downs take linked_cell, and lists and drop-downs list_range. A Forms control lives in four parts that have to agree, and they are written and removed together. Removing a shape that runs a macro leaves the macro; removing a chart is not offered. Excel workbooks only; .xlsx, .xlsm and .xlam.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "'add' or 'remove'.",
            "title": "Action",
            "type": "string"
          },
          "cell": {
            "default": "",
            "description": "For add: the top-left cell, such as B2.",
            "title": "Cell",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "geometry": {
            "default": "",
            "description": "For kind='shape': the preset, such as rect, roundRect or ellipse.",
            "title": "Geometry",
            "type": "string"
          },
          "height": {
            "default": 0.0,
            "description": "Points. 0 picks one.",
            "minimum": 0,
            "title": "Height",
            "type": "number"
          },
          "image_path": {
            "default": "",
            "description": "For kind='picture': a PNG, JPEG or GIF file.",
            "title": "Image Path",
            "type": "string"
          },
          "kind": {
            "default": "button",
            "description": "For add: button, checkBox, optionButton, dropDown, listBox, scrollBar, spinner, label, groupBox, shape, textBox, line or picture.",
            "title": "Kind",
            "type": "string"
          },
          "left": {
            "default": -1.0,
            "description": "For add without cell: points from the left.",
            "title": "Left",
            "type": "number"
          },
          "linked_cell": {
            "default": "",
            "description": "The cell a control writes its value to: $D$6.",
            "title": "Linked Cell",
            "type": "string"
          },
          "list_range": {
            "default": "",
            "description": "The cells a list or drop-down offers: $H$1:$H$9.",
            "title": "List Range",
            "type": "string"
          },
          "macro": {
            "default": "",
            "description": "The procedure a click runs: Proc or Module.Proc.",
            "title": "Macro",
            "type": "string"
          },
          "shape_name": {
            "description": "The shape's name: new for add, as listed for remove.",
            "title": "Shape Name",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "text": {
            "default": "",
            "description": "A control's caption, a shape's text, or a picture's alt text.",
            "title": "Text",
            "type": "string"
          },
          "top": {
            "default": -1.0,
            "description": "For add without cell: points from the top.",
            "title": "Top",
            "type": "number"
          },
          "width": {
            "default": 0.0,
            "description": "Points. 0 picks one.",
            "minimum": 0,
            "title": "Width",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "action",
          "shape_name"
        ],
        "title": "manage_shapeArguments",
        "type": "object"
      },
      "name": "xlide_manage_shape",
      "title": "Add or remove a shape or button"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Adds, removes, renames, moves, hides, shows, protects or unprotects a worksheet. Renaming rewrites the formulas and defined names that referred to the old name, so nothing breaks. Removing a sheet takes its cells, tables and everything on it, and formulas elsewhere that pointed at it become #REF!, which has no undo: ask the user first. Protecting a sheet stops editing in Excel; it is not a security boundary and the password is trivially recovered. Works on .xlsx, .xlsm, .xlam.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "add, remove, rename, move, hide, show, protect or unprotect.",
            "title": "Action",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "index": {
            "default": -1,
            "description": "For add and move: 0-based position. -1 means the end.",
            "title": "Index",
            "type": "integer"
          },
          "new_name": {
            "default": "",
            "description": "For rename: the new name.",
            "title": "New Name",
            "type": "string"
          },
          "password": {
            "default": "",
            "description": "For protect: an optional password. Excel's sheet password is obfuscation, not encryption, so do not use one the user relies on elsewhere.",
            "title": "Password",
            "type": "string"
          },
          "sheet": {
            "default": "",
            "description": "The sheet to act on. For add, the name to give the new one.",
            "title": "Sheet",
            "type": "string"
          },
          "very_hidden": {
            "default": false,
            "description": "For hide: hide it so that Excel's own Unhide dialog does not list it. Only the VBA editor can bring it back.",
            "title": "Very Hidden",
            "type": "boolean"
          }
        },
        "required": [
          "file_path",
          "action"
        ],
        "title": "manage_sheetArguments",
        "type": "object"
      },
      "name": "xlide_manage_sheet",
      "title": "Add, remove or change a worksheet"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, adds or removes Excel tables, the ListObjects that Ctrl+T creates. A table names a block so formulas can say Sales[Amount] instead of an address that breaks when rows move, and it is what a Power Query load writes into. action='list' reads them and changes nothing. Adding one takes the column names from the header row of the range you give. Removing one leaves the cells and takes the table, so structured references to it break.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, add or remove.",
            "title": "Action",
            "type": "string"
          },
          "cell_range": {
            "default": "",
            "description": "For add: the range including its header row, such as A1:D20.",
            "title": "Cell Range",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "sheet": {
            "default": "",
            "description": "Worksheet. Empty lists every sheet's tables.",
            "title": "Sheet",
            "type": "string"
          },
          "table_name": {
            "default": "",
            "description": "For add and remove: the table's name.",
            "title": "Table Name",
            "type": "string"
          },
          "totals_row": {
            "default": false,
            "description": "For add: give the table a totals row.",
            "title": "Totals Row",
            "type": "boolean"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "manage_tableArguments",
        "type": "object"
      },
      "name": "xlide_manage_table",
      "title": "Excel tables"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Lists, adds or clears data validation: what a cell will accept, and the dropdown it shows. kind='list' with formula1 as a comma-separated set of values gives a dropdown; kind='list' with a range reference gives one driven by cells. The other kinds take an operator and one or two formulas, which may be literals or references. Validation stops typing in Excel, not writing through this server, and Excel does not re-check cells that already held a value.",
      "input_schema": {
        "properties": {
          "action": {
            "default": "list",
            "description": "list, add or clear.",
            "title": "Action",
            "type": "string"
          },
          "allow_blank": {
            "default": true,
            "description": "Let the cell be left empty.",
            "title": "Allow Blank",
            "type": "boolean"
          },
          "cell_range": {
            "default": "",
            "description": "For add and clear: the range. Empty clears the whole sheet.",
            "title": "Cell Range",
            "type": "string"
          },
          "error_message": {
            "default": "",
            "description": "What Excel says when the entry is refused.",
            "title": "Error Message",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "formula1": {
            "default": "",
            "description": "The allowed values. For kind='list', either 'Red,Green,Blue' or a range like $H$1:$H$9. For the others, the bound, such as 0.",
            "title": "Formula1",
            "type": "string"
          },
          "formula2": {
            "default": "",
            "description": "The second bound, for between and notBetween.",
            "title": "Formula2",
            "type": "string"
          },
          "kind": {
            "default": "list",
            "description": "whole, decimal, list, date, time, textLength or custom.",
            "title": "Kind",
            "type": "string"
          },
          "operator": {
            "default": "between",
            "description": "between, greaterThan, lessThan and so on.",
            "title": "Operator",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "manage_validationArguments",
        "type": "object"
      },
      "name": "xlide_manage_validation",
      "title": "Data validation"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": false,
        "read_only": false
      },
      "description": "Opens a file in its Office application on the user's screen, for them to see or work in: after a change they should look at, or to put back a copy xlide_close_in_app closed. Already open, it is brought forward instead. read_only opens a copy that cannot be saved over the file, and new_instance a separate Excel or Word the user's other files are not in. bring_to_front=false reopens it behind whatever the user is doing. Macros follow the user's Trust Center settings, as for a file they open themselves. Windows only.",
      "input_schema": {
        "properties": {
          "bring_to_front": {
            "default": true,
            "description": "Bring its window forward. False leaves it behind.",
            "title": "Bring To Front",
            "type": "boolean"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "new_instance": {
            "default": false,
            "description": "Open it in a new instance of the application rather than the one running. PowerPoint has only one; Access always gets its own.",
            "title": "New Instance",
            "type": "boolean"
          },
          "read_only": {
            "default": false,
            "description": "Open a copy that cannot be saved over it.",
            "title": "Read Only",
            "type": "boolean"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "open_in_appArguments",
        "type": "object"
      },
      "name": "xlide_open_in_app",
      "title": "Open in Office"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reads how a worksheet is set up to print: orientation, paper size, margins, the print area, the rows or columns repeated on every page, whether it is scaled to fit, and the header and footer. Call it when asked why a sheet prints the way it does, or before changing a layout somebody set up deliberately.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet"
        ],
        "title": "page_setupArguments",
        "type": "object"
      },
      "name": "xlide_page_setup",
      "title": "How a sheet prints"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Everything about one Office file in a single call: its VBA modules with kinds and line counts, its UserForms, its Power Query queries, its worksheets with used ranges and named ranges, and whether the VBA project is password-protected or digitally signed. has_vba_project is false for a macro-enabled file saved before its first macro, which is normal: the first xlide_write_module gives it a project. Call this once per file before working on it. Each module carries a content_token for a guarded write.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "project_infoArguments",
        "type": "object"
      },
      "name": "xlide_project_info",
      "title": "Project summary"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reads a range of cells from a worksheet and returns a grid of values, formulas, both, or the text each cell shows under its number format. Values are what Excel last calculated and stored, so a formula whose inputs changed outside Excel shows its old result; calculate=true works every formula out first with pyOfficeEditor's formula engine, in memory, and names any cell it could not work out. Ask for formulas to understand what a sheet computes, and values for what it shows. At most 20,000 cells per call.",
      "input_schema": {
        "properties": {
          "calculate": {
            "default": false,
            "description": "Work every formula out before reading, with the formula engine. The file is not changed. Use it after writing inputs, to see the results now.",
            "title": "Calculate",
            "type": "boolean"
          },
          "cell_range": {
            "description": "A1-style range, such as A1:D50, or a single cell.",
            "title": "Cell Range",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "include": {
            "default": "values",
            "description": "'values', 'formulas', 'both', 'text' for what each cell shows, such as 12.5% or 1/15/2024, or 'rich_text' for the font runs of text written in more than one font.",
            "title": "Include",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds allowed if Excel has to answer.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "cell_range"
        ],
        "title": "read_cellsArguments",
        "type": "object"
      },
      "name": "xlide_read_cells",
      "title": "Read cells"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reads one form's design: every control with its name, type, the container it sits in, and the properties the developer set. Properties left at their default are not stored and so are not listed. Use it to understand a form's layout, or to see which control an event procedure belongs to.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "form_name": {
            "description": "Form name, matched without case.",
            "title": "Form Name",
            "type": "string"
          },
          "include_properties": {
            "default": true,
            "description": "Include each control's set properties. Off gives just the tree.",
            "title": "Include Properties",
            "type": "boolean"
          }
        },
        "required": [
          "file_path",
          "form_name"
        ],
        "title": "read_formArguments",
        "type": "object"
      },
      "name": "xlide_read_form",
      "title": "Read form design"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "The canonical way to read VBA. Returns a module's source as the VBA editor shows it, with the attribute header stripped, plus a content_token. Pass that token back as expected_content_token when you write, and the write is refused if anything changed the module in between. start_line and end_line read a slice of a long module; both are 1-based and inclusive.",
      "input_schema": {
        "properties": {
          "end_line": {
            "default": 0,
            "description": "Last line to return. 0 means the end.",
            "minimum": 0,
            "title": "End Line",
            "type": "integer"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "include_header": {
            "default": false,
            "description": "Include the Attribute VB_* header. Off by default: the header is managed for you on write, and editing it by hand is how a module loses its binding to a sheet or a form.",
            "title": "Include Header",
            "type": "boolean"
          },
          "module_name": {
            "description": "Module name, matched without case.",
            "title": "Module Name",
            "type": "string"
          },
          "start_line": {
            "default": 0,
            "description": "First line to return. 0 means the start.",
            "minimum": 0,
            "title": "Start Line",
            "type": "integer"
          }
        },
        "required": [
          "file_path",
          "module_name"
        ],
        "title": "read_moduleArguments",
        "type": "object"
      },
      "name": "xlide_read_module",
      "title": "Read module"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Reads one query's M formula, with its description, group, load target and refresh settings. The formula is the whole let ... in expression as the Advanced Editor shows it.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel workbook.",
            "title": "File Path",
            "type": "string"
          },
          "query_name": {
            "description": "Query name, matched without case.",
            "title": "Query Name",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "query_name"
        ],
        "title": "read_queryArguments",
        "type": "object"
      },
      "name": "xlide_read_query",
      "title": "Read Power Query"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Renames a VBA module everywhere its name is stored, and saves the file. Calls to the module's procedures elsewhere in the project are not rewritten; search for the old name first with xlide_search_modules. Document modules such as ThisWorkbook and Sheet1 cannot be renamed, because the host owns them.",
      "input_schema": {
        "properties": {
          "allow_invalidate_signature": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "module_name": {
            "description": "Module to rename.",
            "title": "Module Name",
            "type": "string"
          },
          "new_name": {
            "description": "New name. A valid VBA identifier.",
            "title": "New Name",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "module_name",
          "new_name"
        ],
        "title": "rename_moduleArguments",
        "type": "object"
      },
      "name": "xlide_rename_module",
      "title": "Rename module"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "The analyzer's rule catalogue: every diagnostic code with its title, default severity, category, whether it mirrors a VBA compile failure, and the MS-VBAL section it enforces. Use it to explain a code to the user, or to decide whether a finding is a compile error or a judgement call.",
      "input_schema": {
        "properties": {
          "code": {
            "default": "",
            "description": "One rule code. Empty lists them all.",
            "title": "Code",
            "type": "string"
          },
          "search": {
            "default": "",
            "description": "Only rules whose code or title contains this.",
            "title": "Search",
            "type": "string"
          }
        },
        "title": "rulesArguments",
        "type": "object"
      },
      "name": "xlide_rules",
      "title": "Analyzer rules"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": false,
        "read_only": false
      },
      "description": "Runs a procedure that already exists in an Office file, in an application this server starts and owns, under a deadline. Windows only, with the application installed. The document opens read-only unless read_only=false, and closes without saving unless save=true. Returns the procedure's return value, anything it logged, and, on a VBA error, the error number and message. The failing line and call stack come from instrumentation applied to injected source, so a procedure already in the document does not carry them; run the same code through xlide_run_vba when you need them. A run that exceeds its timeout reports 'timeout' and the application is terminated. Ask the user before running a macro that changes data.",
      "input_schema": {
        "properties": {
          "args": {
            "anyOf": [
              {
                "items": {},
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Arguments, in order. Scalars only.",
            "title": "Args"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "procedure": {
            "description": "Procedure to call, as 'Proc' or 'Module.Proc'.",
            "title": "Procedure",
            "type": "string"
          },
          "read_only": {
            "default": true,
            "description": "Open the document read-only. False lets the macro change it.",
            "title": "Read Only",
            "type": "boolean"
          },
          "save": {
            "default": false,
            "description": "Save the document after the run. Needs read_only=false.",
            "title": "Save",
            "type": "boolean"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds before the run is terminated.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "procedure"
        ],
        "title": "run_macroArguments",
        "type": "object"
      },
      "name": "xlide_run_macro",
      "title": "Run a macro"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": false,
        "read_only": false
      },
      "description": "Runs the VBA tests in an Office file: every zero-argument procedure whose name starts with Test. Windows only, with the application installed. Each test is run on its own, so one that hangs is reported as a timeout and the rest still run. Returns pass or fail per test with the assertion message, failing line, call stack and any logged output. Use PyVbaAssert and PyVbaAssertEqual in the tests; the harness supplies them.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "module_name": {
            "default": "",
            "description": "Run only this module's tests. Empty runs every module's.",
            "title": "Module Name",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds allowed for each test.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "run_testsArguments",
        "type": "object"
      },
      "name": "xlide_run_tests",
      "title": "Run VBA tests"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": false,
        "read_only": false
      },
      "description": "Injects VBA source into a document and calls one procedure from it, under a deadline. Windows only. Use it to check what a piece of code actually does before writing it into a file, or to read something out of a document that no existing macro exposes. With no file_path it runs in a new empty document. Errors come back as data with the failing line and call stack, never as a dialog. Call PyVbaLog \"text\" from the code to return output; Debug.Print and MsgBox do not work under automation.",
      "input_schema": {
        "properties": {
          "args": {
            "anyOf": [
              {
                "items": {},
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Arguments, in order. Scalars only.",
            "title": "Args"
          },
          "file_path": {
            "default": "",
            "description": "Run against this document instead of a new empty one.",
            "title": "File Path",
            "type": "string"
          },
          "host": {
            "default": "excel",
            "description": "'excel', 'word', 'powerpoint' or 'access'. Ignored with file_path.",
            "title": "Host",
            "type": "string"
          },
          "procedure": {
            "default": "",
            "description": "Procedure to call. Empty calls the first one in the source.",
            "title": "Procedure",
            "type": "string"
          },
          "read_only": {
            "default": true,
            "description": "Open the document read-only.",
            "title": "Read Only",
            "type": "boolean"
          },
          "source": {
            "description": "VBA source to inject.",
            "title": "Source",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds before the run is terminated.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "source"
        ],
        "title": "run_vbaArguments",
        "type": "object"
      },
      "name": "xlide_run_vba",
      "title": "Run VBA source"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Searches every module's source in an Office file and returns each match with its module, line number and the line itself. Use it to find where a name is declared or used before changing it. Plain text by default; set is_regex for a Python regular expression.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "is_regex": {
            "default": false,
            "description": "Treat query as a regular expression.",
            "title": "Is Regex",
            "type": "boolean"
          },
          "match_case": {
            "default": false,
            "description": "Match case. Off by default, because VBA itself ignores case.",
            "title": "Match Case",
            "type": "boolean"
          },
          "max_results": {
            "default": 200,
            "description": "Stop after this many matches.",
            "maximum": 2000,
            "minimum": 1,
            "title": "Max Results",
            "type": "integer"
          },
          "query": {
            "description": "Text or pattern to find.",
            "title": "Query",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "query"
        ],
        "title": "search_modulesArguments",
        "type": "object"
      },
      "name": "xlide_search_modules",
      "title": "Search VBA"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Changes which macro an existing shape or button runs when clicked, and saves the workbook. An empty macro clears the link. Use it after writing a Sub, so a button actually calls it, and after renaming one, because nothing rewrites an OnAction. Give the procedure as Proc or Module.Proc; it must already exist in the project, so write it first. To add a button with its macro, or to remove one, use xlide_manage_shape.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "macro": {
            "description": "The procedure to run, as Proc or Module.Proc. Empty clears the link.",
            "title": "Macro",
            "type": "string"
          },
          "shape_name": {
            "description": "Shape name, as xlide_list_shapes reports it.",
            "title": "Shape Name",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet the shape is on.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "shape_name",
          "macro"
        ],
        "title": "set_shape_macroArguments",
        "type": "object"
      },
      "name": "xlide_set_shape_macro",
      "title": "Point a shape at a macro"
    },
    {
      "annotations": {
        "destructive": false,
        "idempotent": true,
        "read_only": true
      },
      "description": "Checks a file's VBA project for structural problems: records that disagree with each other, a module the directory names but the container does not hold, and the like. This is about the container, not about the code; use xlide_analyze for the code. Worth calling before risky work on an old or repaired file.",
      "input_schema": {
        "properties": {
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ],
        "title": "validate_projectArguments",
        "type": "object"
      },
      "name": "xlide_validate_project",
      "title": "Validate VBA project"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Writes a rectangular block of values and formulas into a worksheet, starting at one cell, and saves the file. Each row of data is a row of the sheet. A string starting with '=' is written as a formula, as you would type it into Excel 365 with no _xlfn prefixes; anything else is a value. A cell given as {\"rich_text\": [{\"text\": \"Total \", \"bold\": true}, {\"text\": \"42\"}]} is text in more than one font. A value written over a formula removes that formula, which is what typing into the cell does. Only the rows you touch are rewritten, so charts, styles, pivot caches and the VBA project are untouched. Ask the user before overwriting cells that hold data. The file keeps no calculated result until Excel next opens it; xlide_read_cells with calculate=true works the results out now.",
      "input_schema": {
        "properties": {
          "data": {
            "description": "Rows of cell values. Numbers, strings, booleans, null for empty, strings starting with '=' for formulas, and {\"rich_text\": [runs]} for text in several fonts, each run a text with any of bold, italic, strike, underline, size, font, color (RRGGBB) and script.",
            "items": {
              "items": {},
              "type": "array"
            },
            "title": "Data",
            "type": "array"
          },
          "file_path": {
            "description": "Absolute path to the Excel file.",
            "title": "File Path",
            "type": "string"
          },
          "sheet": {
            "description": "Worksheet name, matched without case.",
            "title": "Sheet",
            "type": "string"
          },
          "start_cell": {
            "description": "Top-left cell of the block, such as B2.",
            "title": "Start Cell",
            "type": "string"
          },
          "timeout": {
            "default": 0,
            "description": "Seconds allowed if Excel has to do the write.",
            "minimum": 0,
            "title": "Timeout",
            "type": "number"
          }
        },
        "required": [
          "file_path",
          "sheet",
          "start_cell",
          "data"
        ],
        "title": "write_cellsArguments",
        "type": "object"
      },
      "name": "xlide_write_cells",
      "title": "Write cells"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "The canonical way to change VBA. Writes a module's source into the Office file and saves it. It replaces the module's whole source, so send all of it; send the body only, because the attribute header is managed for you. A module that does not exist is created: standard by default, or a class with kind='class'. Pass expected_content_token from your read and the write is refused if the module changed since. The result carries a diff of what the file now holds, read back after saving, which is what to show the user when they ask what changed. After writing, call xlide_analyze and treat any error as a build failure. Ask the user first when the project is protected or signed.",
      "input_schema": {
        "properties": {
          "allow_invalidate_signature": {
            "default": false,
            "description": "Write to a digitally signed project, dropping the signature. Ask the user first.",
            "title": "Allow Invalidate Signature",
            "type": "boolean"
          },
          "allow_protected": {
            "default": false,
            "description": "Write to a password-protected project. Ask the user first.",
            "title": "Allow Protected",
            "type": "boolean"
          },
          "expected_content_token": {
            "default": "",
            "description": "The content_token from your read. Leave empty only when creating a module.",
            "title": "Expected Content Token",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Office file.",
            "title": "File Path",
            "type": "string"
          },
          "include_diff": {
            "default": true,
            "description": "Include a unified diff of what changed. Off in a loop that writes many modules and reads none of them back.",
            "title": "Include Diff",
            "type": "boolean"
          },
          "kind": {
            "default": "standard",
            "description": "Kind for a module being created: 'standard' or 'class'.",
            "title": "Kind",
            "type": "string"
          },
          "module_name": {
            "description": "Module to write, or to create.",
            "title": "Module Name",
            "type": "string"
          },
          "source": {
            "description": "The module's complete VBA source.",
            "title": "Source",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "module_name",
          "source"
        ],
        "title": "write_moduleArguments",
        "type": "object"
      },
      "name": "xlide_write_module",
      "title": "Write module"
    },
    {
      "annotations": {
        "destructive": true,
        "idempotent": true,
        "read_only": false
      },
      "description": "Changes a workbook's Power Query and saves it. action='set' replaces a query's M formula, creating it if it does not exist; 'rename' renames it and rewrites the queries that reference it by name; 'remove' deletes it and everything that loaded it onto a sheet, which has no undo, so ask the user first. 'load' puts a query's result on a worksheet and 'unload' takes it back off. Loading needs the column names, because writing the connection means naming the columns and knowing them means running the query, which nothing here does; Excel settles them against the real result on the first refresh. A query already loaded keeps its rows until Excel refreshes it.",
      "input_schema": {
        "properties": {
          "action": {
            "description": "'set', 'rename', 'remove', 'load' or 'unload'.",
            "title": "Action",
            "type": "string"
          },
          "cell": {
            "default": "A1",
            "description": "For load: the table's top-left cell.",
            "title": "Cell",
            "type": "string"
          },
          "columns": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "For load: the column names the query returns, in order. Required, because the connection has to name them and nothing here runs the query to find out. Excel corrects them on the first refresh.",
            "title": "Columns"
          },
          "description": {
            "default": "",
            "description": "For set: the query's description.",
            "title": "Description",
            "type": "string"
          },
          "file_path": {
            "description": "Absolute path to the Excel workbook.",
            "title": "File Path",
            "type": "string"
          },
          "formula": {
            "default": "",
            "description": "For set: the whole M expression, such as 'let Source = 1 in Source'.",
            "title": "Formula",
            "type": "string"
          },
          "group": {
            "default": "",
            "description": "For set on a new query: the folder in the Queries pane.",
            "title": "Group",
            "type": "string"
          },
          "include_diff": {
            "default": true,
            "description": "For set: include a unified diff of the M that changed. Nothing to diff for the other actions.",
            "title": "Include Diff",
            "type": "boolean"
          },
          "new_name": {
            "default": "",
            "description": "For rename: the new name.",
            "title": "New Name",
            "type": "string"
          },
          "query_name": {
            "description": "The query to change.",
            "title": "Query Name",
            "type": "string"
          },
          "sheet": {
            "default": "",
            "description": "For load: the worksheet. Empty uses the first.",
            "title": "Sheet",
            "type": "string"
          }
        },
        "required": [
          "file_path",
          "action",
          "query_name"
        ],
        "title": "write_queryArguments",
        "type": "object"
      },
      "name": "xlide_write_query",
      "title": "Write Power Query"
    }
  ]
}
